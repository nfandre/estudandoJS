<h4>Categorias: </h4>
{{#each categorias}}
    <div class="card">
        <div class="card-body">
            <a href=""> <h4>{{nome}}</h4></a>
           
            <hr>
            <small>Data de publicação: {{date}}</small>
            <hr>
            {{slug}}
        </div>
    </div>
{{else}}


{{/each}}

